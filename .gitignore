# Ignore everything
*

# Allow Public/ (the deploy target) and its contents
!Public/
!Public/**

# Allow config files needed for deployment
!netlify.toml
!.gitignore

# OS files (always ignore even inside Public/)
.DS_Store
Thumbs.db
Desktop.ini

# Large video files (consider Git LFS if these need versioning)
*.MOV
*.mp4
